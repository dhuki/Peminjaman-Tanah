<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ddr.peminjamantanah">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    //fungsi ini digunakan untuk mendaftarkan permission pada aplikasi kita sehingga permission pada aplikasi kita hanya Read Storage

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Activity.LoginActivity"
            android:theme="@style/LoginTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity.MenuUtamaUserActivity" />
        <activity android:name=".Activity.InformasiSewa" />
        <activity
            android:name=".Activity.PreRegistration"
            android:theme="@style/LoginTheme" />
        <activity
            android:name=".Activity.Registration"
            android:theme="@style/LoginTheme" />
        <activity android:name=".Activity.PerpanjangnSewa"></activity>
    </application>

</manifest>